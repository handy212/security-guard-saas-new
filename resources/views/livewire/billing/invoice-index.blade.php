<div class="p-6 space-y-5"><h1 class="text-2xl font-bold">Billing & Invoices</h1><form wire:submit="generate" class="flex gap-3 rounded-xl border bg-white p-4"><select wire:model="clientId" class="rounded border p-2"><option value="">Client</option>@foreach($clients as $client)<option value="{{ $client->id }}">{{ $client->name }}</option>@endforeach</select><input wire:model="month" type="month" class="rounded border p-2"><button class="rounded bg-slate-900 px-4 py-2 text-white">Generate Monthly Invoice</button></form><div class="overflow-auto rounded-xl border bg-white"><table class="w-full text-sm"><thead><tr class="bg-slate-50 text-left"><th class="p-3">Invoice</th><th>Client</th><th>Date</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@foreach($invoices as $invoice)<tr class="border-t"><td class="p-3 font-medium">{{ $invoice->invoice_number }}</td><td>{{ $invoice->clientAccount?->name }}</td><td>{{ $invoice->invoice_date }}</td><td>{{ $invoice->grand_total }}</td><td>{{ $invoice->status }}</td><td><button wire:click="markSent({{ $invoice->id }})" class="text-blue-600">Mark Sent</button></td></tr>@endforeach</tbody></table></div></div>
