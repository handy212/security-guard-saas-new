<div class="p-6 space-y-5"><h1 class="text-2xl font-bold">Roles & Permissions</h1><form wire:submit="createRole" class="flex gap-3 rounded-xl border bg-white p-4"><input wire:model="roleName" class="rounded border p-2" placeholder="Role name"><button class="rounded bg-slate-900 px-4 py-2 text-white">Create Role</button></form><div class="grid gap-4">@foreach($roles as $role)<div class="rounded-xl border bg-white p-4"><div class="flex justify-between"><b>{{ $role->name }}</b><button wire:click="sync({{ $role->id }})" class="rounded bg-slate-900 px-3 text-white">Save Permissions</button></div><div class="mt-3 grid gap-2 md:grid-cols-4">@foreach($allPermissions as $permission)<label class="text-sm"><input type="checkbox" wire:model="permissions.{{ $role->id }}" value="{{ $permission->name }}"> {{ $permission->name }}</label>@endforeach</div></div>@endforeach</div></div>
